<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication001">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".mApplication"
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="即刻美食"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UI.Activity.Activity.ProductListActivity"></activity>
        <activity android:name=".UI.Activity.Activity.BaseActivity" />
     <activity android:name=".UI.Activity.Activity.OrderActivity" />
        <activity android:name=".UI.Activity.Activity.RegistActivity" />
        <activity android:name=".UI.Activity.Activity.LoginActivity" />
        <activity android:name=".UI.Activity.Activity.MainActivity" />
        <activity
            android:name=".UI.Activity.Activity.SplashActivity"
            android:theme="@style/AppTheme_FullScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>